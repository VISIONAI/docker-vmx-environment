docker-vmx-environment
======================

The library dependencies for the VMX executable(s). Contains
ca-certificates so that images can be retrieved from https hosts.

This Dockerfile is part of the VMX package. To install VMX on Linux
take a look at https://github.com/VISIONAI/vmx-docker-manager
